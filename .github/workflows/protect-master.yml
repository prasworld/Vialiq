name: Apply Master Branch Protection

# ─────────────────────────────────────────────────────────────────────────────
# Run this workflow ONCE (manually) after adding the GH_PAT secret, or whenever
# you need to re-apply the branch protection rules (e.g. after repo migration).
#
# What it enforces on `master`:
#   • Direct pushes are blocked — changes must arrive via Pull Request.
#   • PR must have at least 1 approving review.
#   • The CI status check must pass (controlled by the `ci_job_name` input below).
#   • Force-pushes are disabled.
#   • Branch cannot be deleted.
#   • Rules apply to admins too (dismisses the "I'm an admin" bypass loophole).
#
# IMPORTANT — keeping the CI job name in sync:
#   The `ci_job_name` input must exactly match the `jobs.<id>` key in ci.yml.
#   Current value: "check"  (see .github/workflows/ci.yml → jobs: check:)
#   If you rename that job, re-run this workflow with the updated input value.
#
# The GH_PAT secret must have:
#   • Fine-grained token: Contents (Read & Write) + Administration (Read & Write).
# ─────────────────────────────────────────────────────────────────────────────

on:
  workflow_dispatch:
    inputs:
      ci_job_name:
        description: >
          Exact job ID from ci.yml whose status must pass before a PR can merge.
          Must match `jobs.<id>` in .github/workflows/ci.yml.
        required: true
        default: check   # <── keep in sync with ci.yml → jobs: check:

jobs:
  protect:
    runs-on: ubuntu-latest
    permissions:
      contents: read  # only needs to read; Administration API uses the PAT

    env:
      # Single source of truth for the required check name inside this job.
      CI_JOB_NAME: ${{ github.event.inputs.ci_job_name }}

    steps:
      - name: Apply branch protection to master
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
          REPO: ${{ github.repository }}
        run: |
          echo "Applying branch protection — required status check: '$CI_JOB_NAME'"

          HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
            -X PUT \
            -H "Authorization: Bearer $GH_PAT" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/$REPO/branches/master/protection" \
            -d "{
              \"required_status_checks\": {
                \"strict\": true,
                \"contexts\": [\"$CI_JOB_NAME\"]
              },
              \"enforce_admins\": true,
              \"required_pull_request_reviews\": {
                \"dismiss_stale_reviews\": true,
                \"require_code_owner_reviews\": false,
                \"required_approving_review_count\": 1
              },
              \"restrictions\": null,
              \"allow_force_pushes\": false,
              \"allow_deletions\": false,
              \"block_creations\": false,
              \"required_conversation_resolution\": true,
              \"lock_branch\": false
            }")

          if [ "$HTTP_STATUS" != "200" ]; then
            echo "::error::GitHub API returned HTTP $HTTP_STATUS. Check your GH_PAT permissions."
            exit 1
          fi
          echo "✅ Branch protection applied to master (HTTP $HTTP_STATUS, check: '$CI_JOB_NAME')"
